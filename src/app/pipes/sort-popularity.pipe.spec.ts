import { SortPopularityPipe } from './sort-popularity.pipe';

describe('SortPopularityPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPopularityPipe();
    expect(pipe).toBeTruthy();
  });
});
